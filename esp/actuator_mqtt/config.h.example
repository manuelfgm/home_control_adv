/*
 * Archivo de configuración para Actuador MQTT
 * ¡NO SUBIR ESTE ARCHIVO A GIT!
 */

#ifndef CONFIG_H
#define CONFIG_H

// ===== CONFIGURACIÓN WiFi =====
const char* ssid = "ssid";
const char* password = "pass";

// ===== CONFIGURACIÓN MQTT =====
const char* mqtt_server = "ip";  // IP de tu Raspberry Pi
const int mqtt_port = 1883;
const char* mqtt_user = "";                  // Usuario MQTT (opcional)
const char* mqtt_pass = "";                  // Password MQTT (opcional)

// ===== CONFIGURACIÓN DEL ACTUADOR =====
const char* actuator_id = "boiler";  // ID único del actuador

// ===== CONFIGURACIÓN DE PINES =====
#define RELAY_PIN 0                          // Pin del relé
#define LED_PIN 2                            // LED indicador

// ===== CONFIGURACIÓN DE TIMING =====
const unsigned long statusInterval = 60000;  // 1 minuto entre reportes de estado

// ===== CONFIGURACIÓN DE SEGURIDAD =====
const float emergencyShutoffTemp = 25.0;     // Temperatura de emergencia
const unsigned long overflowRestart = 0xffff0000;  // Evitar desborde unsigned long

#endif // CONFIG_H